version: 1
#backend:
#  phases:
#    build:
#      commands:
#        - amplifyPush --simple
#        - chmod u+x ./myamplifyPush.sh
#        - ./myamplifyPush.sh
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
